/**
 * Vinato — Runtime Environment Configuration
 *
 * For local development: fill in the values below.
 * For production: inject these via your hosting platform's environment
 * variable injection (e.g., a CI/CD step that rewrites this file at build time,
 * or a server-side template that sets window.__SUPABASE_URL__ before this loads).
 *
 * IMPORTANT: Do NOT commit real credentials here. This file is intentionally
 * left with empty placeholders. Add env.js to your .gitignore if you populate it locally.
 */
window.__SUPABASE_URL__      = '';   // e.g. 'https://xxxx.supabase.co'
window.__SUPABASE_ANON_KEY__ = '';   // e.g. 'eyJ...'
